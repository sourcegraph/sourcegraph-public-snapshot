import { createBrowserHistory } from 'history'

/**
 * TODO(valery): RR6
 * @deprecated this global history variable should be removed once we complete the migration to react-router 6.
 */
export const globalHistory = createBrowserHistory()
