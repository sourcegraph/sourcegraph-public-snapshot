/**
 * Common props for sentinel consumer components (the homepage, the directory page)
 * These props are needed for condition rendering of Sentinel.
 */
export interface SentinelProps {
    sentinelEnabled?: boolean
}
