{
  "private": true,
  "name": "@sourcegraph/utils",
  "version": "0.0.1",
  "description": "Sourcegraph utils",
  "main": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "eslint": "eslint --cache '**/*.[jt]s?(x)'",
    "stylelint": "stylelint 'src/**/*.scss' --quiet",
    "test": "jest",
    "build-ts": "tsc -b tsconfig.json"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
