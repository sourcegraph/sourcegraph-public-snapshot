load("@aspect_rules_ts//ts:defs.bzl", "ts_config")
load("@npm//:defs.bzl", "npm_link_all_packages")
load("//dev:defs.bzl", "npm_package", "ts_project")

npm_link_all_packages(name = "node_modules")

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    visibility = ["//client:__subpackages__"],
    deps = [
        "//:tsconfig",
        "//client/build-config:tsconfig",
    ],
)

ts_project(
    name = "testing_lib",
    srcs = [
        "src/aria-asserts.ts",
        "src/index.ts",
        "src/percySnapshotToDisk.ts",
        "src/types/@percy/logger/index.d.ts",
        "src/types/@percy/sdk-utils/index.d.ts",
    ],
    tsconfig = ":tsconfig",
    deps = [
        ":node_modules/@sourcegraph/build-config",
        "//:node_modules/@percy/sdk-utils",
        "//:node_modules/@types/jest",  #keep
        "//:node_modules/@types/node",
        "//:node_modules/@types/puppeteer",
        "//:node_modules/puppeteer",
    ],
)

npm_package(
    name = "testing_pkg",
    srcs = [
        "package.json",
        ":testing_lib",
    ],
)
