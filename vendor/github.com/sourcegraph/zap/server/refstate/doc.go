// Package refstate provides state storage and management for
// individual refs on a Zap server (stored in a refdb).
//
// It differs from refdb in that it represents an individual ref's
// data, while refdb handles ref name lookups and symbolic ref
// solution.
package refstate
