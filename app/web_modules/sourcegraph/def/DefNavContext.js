import React from "react";

export default class DefNavContext extends React.Component {
	render() {
		// It would be nice to show the name of the def, but that
		// gets pretty complicated, so instead show no special
		// context (just the ordinary repo breadcrumb).
		return null;
	}
}
