#!/bin/bash
set -e

./node_modules/.bin/tcm web_modules/

for f in $(find ./web_modules -name '*.css.d.ts'); do
	echo -e '// generated by "npm run generate"\n// tslint:disable\n' | cat - $f > $f~
	mv $f~ $f
done
