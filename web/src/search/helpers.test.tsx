// @ts-ignore
import assert from 'assert'

describe('search/helpers', () => {
    describe('queryIndexOfScope()', () => {
        test('should return the index of a scope if contained in the query')
        test('should return the index of a scope if at the beginning of the query')
        test('should return the -1 if the scope is not contained in the query')
        test('should return the -1 if the scope is contained as a substring of another scope')
    })
})
