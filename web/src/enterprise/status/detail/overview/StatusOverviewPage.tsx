import React from 'react'
import { StatusAreaContext } from '../StatusArea'

interface Props extends StatusAreaContext {}

/**
 * An overview of a status.
 */
export const StatusOverviewPage: React.FunctionComponent<Props> = ({}) => 'a'
