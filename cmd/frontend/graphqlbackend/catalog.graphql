extend type Query {
    """
    [WIP]
    """
    catalog: Catalog!
}

type Catalog {
    components(query: String): CatalogComponentConnection!
}

type CatalogComponentConnection {
    nodes: [CatalogComponent!]!
    totalCount: Int!
    pageInfo: PageInfo!
}

type CatalogComponent implements Node {
    id: ID!
    name: String!
    xyz123: String!
}
