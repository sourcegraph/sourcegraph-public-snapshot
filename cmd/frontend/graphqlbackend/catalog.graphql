extend type Query {
    """
    [WIP]
    """
    catalog: Catalog!
}

type Catalog {
    components: CatalogComponentConnection!
}

type CatalogComponentConnection {
    nodes: [CatalogComponent!]!
    totalCount: Int!
    pageInfo: PageInfo!
}

type CatalogComponent implements Node {
    id: ID!
    name: String!
}
