schemaVersion: "2.0.0"

commandTests:
  - name: "binary is runnable"
    command: "/blobstore"
    envVars:
      - key: "SANITY_CHECK"
        value: "true"

  - name: "not running as root"
    command: "id -u | grep -v '0'"
