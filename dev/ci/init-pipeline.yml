steps:
  - command: "go run ./dev/ci/gen-pipeline.go | buildkite-agent pipeline upload"
    label: ":pipeline:"
    plugins:
      gopath-checkout#v1.0.1:
        import: github.com/sourcegraph/sourcegraph
