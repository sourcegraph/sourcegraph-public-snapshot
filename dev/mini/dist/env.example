########################
# GLOBAL VALUES
########################

# SRC_APP_HOST=http://localhost  # public hostname for your Sourcegraph instance (e.g., the hostname of the URL you type into your browser)
# SRC_APP_PORT=3080  # public port for your Sourcegraph instance (e.g., the port of the URL you type into your web browser)

############################
# LOCAL FILESYSTEM SETUP
############################

ORIGIN_MAP=local/!local/%  # the mapping from URL path to directory path in gitserver
# GIT_PARENT_DIRECTORY=/Users/user/path/to/folder  # the directory on the host machine that contains local git repositories
