{
  "name": "release",
  "description": "Scripts for managing release captain duties",
  "private": true,
  "scripts": {
    "release": "yarn run -T ts-node --transpile-only ./src/main.ts",
    "lint:js": "yarn run -T eslint --cache 'src/**/*.[jt]s?(x)'"
  }
}
