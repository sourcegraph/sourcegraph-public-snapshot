{
  "private": true,
  "name": "@sourcegraph/dev-release",
  "version": "0.0.1",
  "description": "Scripts for managing release captain duties",
  "scripts": {
    "release": "pnpm ts-node --transpile-only ./src/main.ts",
    "lint:js": "pnpm eslint --cache 'src/**/*.[jt]s?(x)'"
  }
}
