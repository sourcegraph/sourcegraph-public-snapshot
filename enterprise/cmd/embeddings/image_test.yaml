schemaVersion: "2.0.0"

commandTests:
  - name: "binary is runnable"
    command: "/embeddings"
    envVars:
      - key: "SANITY_CHECK"
        value: "true"
  # TODO: Test currently fails
  # - name: "bash is runnable"
  #   command: "bash"
  #   args:
  #     - --version

  - name: "not running as root"
    command: "/usr/bin/id"
    args:
      - -u
    excludedOutput: ["^0"]
    exitCode: 0
