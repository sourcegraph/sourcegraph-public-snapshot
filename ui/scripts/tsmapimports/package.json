{
  "name": "tsmapimports",
  "version": "0.0.1",
  "description": "TypeScript import remapper: replaces all instances of a selector with something else and adds an import",
  "main": "index.js",
  "devDependencies": {
    "@types/node": "^6.0.45",
    "typescript": "^2.1.0"
  },
  "scripts": {
    "compile": "yarn && tsc -p .",
    "test": "tsc -p . && TEST=1 node index.js"
  }
}
