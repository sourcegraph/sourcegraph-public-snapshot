import Event from 'vs/base/common/event';
import { IDisposable } from 'vs/base/common/lifecycle';
import { IWindowsService } from 'vs/platform/windows/common/windows';
export interface IWindowEventService {
    _serviceBrand: any;
    onNewWindowOpen: Event<number>;
    onWindowFocus: Event<number>;
}
export declare class ActiveWindowManager implements IDisposable {
    private disposables;
    private _activeWindowId;
    constructor(windowsService: IWindowsService);
    private setActiveWindow(windowId);
    readonly activeClientId: string;
    dispose(): void;
}
