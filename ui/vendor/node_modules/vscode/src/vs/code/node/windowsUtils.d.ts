import { OpenContext } from 'vs/code/common/windows';
/**
 * Exported subset of VSCodeWindow for testing.
 */
export interface ISimpleWindow {
    openedWorkspacePath: string;
    lastFocusTime: number;
}
/**
 * Exported for testing.
 */
export interface IBestWindowOrFolderOptions<SimpleWindow extends ISimpleWindow> {
    windows: SimpleWindow[];
    newWindow: boolean;
    reuseWindow: boolean;
    context: OpenContext;
    filePath?: string;
    userHome?: string;
    vscodeFolder?: string;
}
export declare function findBestWindowOrFolder<SimpleWindow extends ISimpleWindow>({windows, newWindow, reuseWindow, context, filePath, userHome, vscodeFolder}: IBestWindowOrFolderOptions<SimpleWindow>): SimpleWindow | string;
export declare function getLastActiveWindow<WINDOW extends ISimpleWindow>(windows: WINDOW[]): WINDOW;
