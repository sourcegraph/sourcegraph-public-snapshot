import { IAction, IActionRunner } from 'vs/base/common/actions';
import { SelectActionItem, IActionItem } from 'vs/base/browser/ui/actionbar/actionbar';
import { EventEmitter } from 'vs/base/common/eventEmitter';
import { IConfigurationService } from 'vs/platform/configuration/common/configuration';
import { ICommandService } from 'vs/platform/commands/common/commands';
import { IDebugService } from 'vs/workbench/parts/debug/common/debug';
export declare class StartDebugActionItem extends EventEmitter implements IActionItem {
    private context;
    private action;
    private debugService;
    private configurationService;
    private commandService;
    private static ADD_CONFIGURATION;
    private static SEPARATOR;
    actionRunner: IActionRunner;
    private container;
    private start;
    private selectBox;
    private toDispose;
    constructor(context: any, action: IAction, debugService: IDebugService, configurationService: IConfigurationService, commandService: ICommandService);
    private registerListeners();
    render(container: HTMLElement): void;
    setActionContext(context: any): void;
    isEnabled(): boolean;
    focus(): void;
    blur(): void;
    dispose(): void;
    private updateOptions();
}
export declare class FocusProcessActionItem extends SelectActionItem {
    private debugService;
    constructor(action: IAction, debugService: IDebugService);
}
