import './media/scmViewlet.css';
import Event from 'vs/base/common/event';
import { IDisposable } from 'vs/base/common/lifecycle';
import { IContextKeyService } from 'vs/platform/contextkey/common/contextkey';
import { IMenuService } from 'vs/platform/actions/common/actions';
import { IAction } from 'vs/base/common/actions';
import { ISCMService, ISCMResource, ISCMResourceGroup } from 'vs/workbench/services/scm/common/scm';
export declare class SCMMenus implements IDisposable {
    private contextKeyService;
    private scmService;
    private menuService;
    private disposables;
    private activeProviderId;
    private titleDisposable;
    private titleActions;
    private titleSecondaryActions;
    private _onDidChangeTitle;
    readonly onDidChangeTitle: Event<void>;
    constructor(contextKeyService: IContextKeyService, scmService: ISCMService, menuService: IMenuService);
    private setActiveProvider(activeProvider);
    getTitleActions(): IAction[];
    getTitleSecondaryActions(): IAction[];
    getResourceGroupActions(group: ISCMResourceGroup): IAction[];
    getResourceGroupContextActions(group: ISCMResourceGroup): IAction[];
    getResourceActions(resource: ISCMResource): IAction[];
    getResourceContextActions(resource: ISCMResource): IAction[];
    private getSCMResourceGroupURI(resourceGroup);
    private getSCMResourceURI(resource);
    private static readonly NoActions;
    private getActions(menuId, context, resourceGroupId);
    dispose(): void;
}
