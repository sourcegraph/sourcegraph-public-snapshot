import { TPromise } from 'vs/base/common/winjs.base';
import { Builder, Dimension } from 'vs/base/browser/builder';
import { IAction } from 'vs/base/common/actions';
import { Viewlet } from 'vs/workbench/browser/viewlet';
import { ITelemetryService } from 'vs/platform/telemetry/common/telemetry';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
export declare class TreeExplorerViewlet extends Viewlet {
    private instantiationService;
    private viewletContainer;
    private view;
    private viewletState;
    private viewletId;
    private treeNodeProviderId;
    constructor(viewletId: string, telemetryService: ITelemetryService, instantiationService: IInstantiationService);
    getId(): string;
    create(parent: Builder): TPromise<void>;
    layout(dimension: Dimension): void;
    setVisible(visible: boolean): TPromise<void>;
    getActions(): IAction[];
    private addTreeView();
    focus(): void;
    shutdown(): void;
    dispose(): void;
}
