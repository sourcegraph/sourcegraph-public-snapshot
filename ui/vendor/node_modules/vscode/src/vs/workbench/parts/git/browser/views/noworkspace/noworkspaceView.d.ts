import './noworkspaceView.css';
import winjs = require('vs/base/common/winjs.base');
import ee = require('vs/base/common/eventEmitter');
import view = require('vs/workbench/parts/git/browser/views/view');
import builder = require('vs/base/browser/builder');
import { IActionRunner, IAction } from 'vs/base/common/actions';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
export declare class NoWorkspaceView extends ee.EventEmitter implements view.IView {
    private actionRunner;
    private instantiationService;
    ID: string;
    private _element;
    private _openFolderButton;
    constructor(actionRunner: IActionRunner, instantiationService: IInstantiationService);
    readonly element: HTMLElement;
    private render();
    focus(): void;
    layout(dimension: builder.Dimension): void;
    setVisible(visible: boolean): winjs.TPromise<void>;
    getControl(): ee.IEventEmitter;
    getActions(): IAction[];
    getSecondaryActions(): IAction[];
}
