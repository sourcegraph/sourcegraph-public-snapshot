import { TPromise } from 'vs/base/common/winjs.base';
import URI from 'vs/base/common/uri';
import { EncodingMode, ConfirmResult } from 'vs/workbench/common/editor';
import { TextFileEditorModel } from 'vs/workbench/services/textfile/common/textFileEditorModel';
import { FileEditorInput as CommonFileEditorInput } from 'vs/workbench/parts/files/common/files';
import { ITextFileService } from 'vs/workbench/services/textfile/common/textfiles';
import { IWorkspaceContextService } from 'vs/platform/workspace/common/workspace';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
/**
 * A file editor input is the input type for the file editor of file system resources.
 */
export declare class FileEditorInput extends CommonFileEditorInput {
    private instantiationService;
    private contextService;
    private textFileService;
    private resource;
    private preferredEncoding;
    private forceOpenAsBinary;
    private name;
    private description;
    private verboseDescription;
    private toUnbind;
    /**
     * An editor input who's contents are retrieved from file services.
     */
    constructor(resource: URI, preferredEncoding: string, instantiationService: IInstantiationService, contextService: IWorkspaceContextService, textFileService: ITextFileService);
    private registerListeners();
    private onDirtyStateChange(e);
    setResource(resource: URI): void;
    getResource(): URI;
    setPreferredEncoding(encoding: string): void;
    getEncoding(): string;
    setEncoding(encoding: string, mode: EncodingMode): void;
    setForceOpenAsBinary(): void;
    getTypeId(): string;
    getName(): string;
    getDescription(verbose?: boolean): string;
    isDirty(): boolean;
    confirmSave(): ConfirmResult;
    save(): TPromise<boolean>;
    revert(): TPromise<boolean>;
    getPreferredEditorId(candidates: string[]): string;
    resolve(refresh?: boolean): TPromise<TextFileEditorModel>;
    dispose(): void;
    matches(otherInput: any): boolean;
}
