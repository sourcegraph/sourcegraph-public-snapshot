import '../browser/media/exceptionWidget.css';
import { ZoneWidget } from 'vs/editor/contrib/zoneWidget/browser/zoneWidget';
import { ICodeEditor } from 'vs/editor/browser/editorBrowser';
import { IContextViewService } from 'vs/platform/contextview/browser/contextView';
import { IDebugService } from 'vs/workbench/parts/debug/common/debug';
export declare class ExceptionWidget extends ZoneWidget {
    private lineNumber;
    private contextViewService;
    private debugService;
    constructor(editor: ICodeEditor, lineNumber: number, contextViewService: IContextViewService, debugService: IDebugService);
    protected _fillContainer(container: HTMLElement): void;
    protected _doLayout(heightInPixel: number, widthInPixel: number): void;
}
