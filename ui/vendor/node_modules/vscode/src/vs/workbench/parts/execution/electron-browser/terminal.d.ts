import { TPromise } from 'vs/base/common/winjs.base';
export declare const DEFAULT_TERMINAL_LINUX_READY: TPromise<string>;
export declare const DEFAULT_TERMINAL_OSX = "Terminal.app";
export declare const DEFAULT_TERMINAL_WINDOWS = "%COMSPEC%";
export interface ITerminalConfiguration {
    terminal: {
        external: {
            linuxExec: string;
            osxExec: string;
            windowsExec: string;
        };
    };
}
