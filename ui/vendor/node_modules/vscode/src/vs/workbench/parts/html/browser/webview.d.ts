import URI from 'vs/base/common/uri';
import Event from 'vs/base/common/event';
export interface WebviewOptions {
    nodeintegration: boolean;
}
export default class Webview {
    private _styleElement;
    private _webview;
    private _ready;
    private _disposables;
    private _onDidClickLink;
    private _onDidLoadContent;
    constructor(parent: HTMLElement, _styleElement: Element, options: WebviewOptions);
    dispose(): void;
    readonly domNode: HTMLElement;
    readonly onDidClickLink: Event<URI>;
    readonly onDidLoadContent: Event<{
        stats: any;
    }>;
    private _send(channel, ...args);
    contents: string[];
    baseUrl: string;
    focus(): void;
    style(themeId: string): void;
}
