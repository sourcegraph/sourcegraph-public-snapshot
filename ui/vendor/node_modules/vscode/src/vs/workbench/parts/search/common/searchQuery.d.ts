import { IPatternInfo, IQueryOptions, ISearchQuery } from 'vs/platform/search/common/search';
import { IConfigurationService } from 'vs/platform/configuration/common/configuration';
export declare class QueryBuilder {
    private configurationService;
    constructor(configurationService: IConfigurationService);
    text(contentPattern: IPatternInfo, options?: IQueryOptions): ISearchQuery;
    file(options?: IQueryOptions): ISearchQuery;
    private query(type, contentPattern, options?);
}
