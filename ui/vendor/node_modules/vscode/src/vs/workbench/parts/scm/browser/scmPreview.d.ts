import { Action } from 'vs/base/common/actions';
import { TPromise } from 'vs/base/common/winjs.base';
import { IWindowService } from 'vs/platform/windows/common/windows';
import { IMessageService } from 'vs/platform/message/common/message';
export default class SCMPreview {
    private static readonly _enabled;
    static enabled: boolean;
}
export declare class EnableSCMPreviewAction extends Action {
    private windowService;
    private messageService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, windowService: IWindowService, messageService: IMessageService);
    run(): TPromise<void>;
}
export declare class DisableSCMPreviewAction extends Action {
    private windowService;
    private messageService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, windowService: IWindowService, messageService: IMessageService);
    run(): TPromise<void>;
}
