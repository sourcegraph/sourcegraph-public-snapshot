import workbenchExt = require('vs/workbench/common/contributions');
import { ISnippetsService } from 'vs/workbench/parts/snippets/electron-browser/snippetsService';
import { ILifecycleService } from 'vs/platform/lifecycle/common/lifecycle';
import { IEnvironmentService } from 'vs/platform/environment/common/environment';
import { IExtensionService } from 'vs/platform/extensions/common/extensions';
import { IModeService } from 'vs/editor/common/services/modeService';
export declare class SnippetsTracker implements workbenchExt.IWorkbenchContribution {
    private lifecycleService;
    private modeService;
    private snippetService;
    private static FILE_WATCH_DELAY;
    private snippetFolder;
    private toDispose;
    private watcher;
    private fileWatchDelayer;
    constructor(lifecycleService: ILifecycleService, modeService: IModeService, snippetService: ISnippetsService, environmentService: IEnvironmentService, extensionService: IExtensionService);
    private registerListeners();
    private scanUserSnippets();
    private unregisterListener();
    getId(): string;
    dispose(): void;
}
