import { TPromise } from 'vs/base/common/winjs.base';
import { Action } from 'vs/base/common/actions';
import uri from 'vs/base/common/uri';
import { IWorkbenchEditorService } from 'vs/workbench/services/editor/common/editorService';
import { IMessageService } from 'vs/platform/message/common/message';
import { IEditorGroupService } from 'vs/workbench/services/group/common/groupService';
import { IWindowsService, IWindowService } from 'vs/platform/windows/common/windows';
export declare class RevealInOSAction extends Action {
    private windowsService;
    private resource;
    constructor(resource: uri, windowsService: IWindowsService);
    run(): TPromise<any>;
}
export declare class GlobalRevealInOSAction extends Action {
    private editorService;
    private windowsService;
    private messageService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, editorService: IWorkbenchEditorService, windowsService: IWindowsService, messageService: IMessageService);
    run(): TPromise<any>;
}
export declare class CopyPathAction extends Action {
    private resource;
    constructor(resource: uri);
    run(): TPromise<any>;
}
export declare class GlobalCopyPathAction extends Action {
    private editorService;
    private editorGroupService;
    private messageService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, editorService: IWorkbenchEditorService, editorGroupService: IEditorGroupService, messageService: IMessageService);
    run(): TPromise<any>;
}
export declare class OpenFileAction extends Action {
    private editorService;
    private windowService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, editorService: IWorkbenchEditorService, windowService: IWindowService);
    run(): TPromise<any>;
}
export declare class OpenFolderAction extends Action {
    private windowService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, windowService: IWindowService);
    run(): TPromise<any>;
}
export declare class OpenFileFolderAction extends Action {
    private windowService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, windowService: IWindowService);
    run(): TPromise<any>;
}
export declare class ShowOpenedFileInNewWindow extends Action {
    private windowsService;
    private editorService;
    private messageService;
    static ID: string;
    static LABEL: string;
    constructor(id: string, label: string, windowsService: IWindowsService, editorService: IWorkbenchEditorService, messageService: IMessageService);
    run(): TPromise<any>;
}
