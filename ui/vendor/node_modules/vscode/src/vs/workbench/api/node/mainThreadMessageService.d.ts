import { IMessageService, IChoiceService } from 'vs/platform/message/common/message';
import Severity from 'vs/base/common/severity';
import { MainThreadMessageServiceShape } from './extHost.protocol';
import * as vscode from 'vscode';
export declare class MainThreadMessageService extends MainThreadMessageServiceShape {
    private _messageService;
    private _choiceService;
    private _disable;
    constructor(_messageService: IMessageService, _choiceService: IChoiceService);
    $showMessage(severity: Severity, message: string, options: vscode.MessageOptions, commands: {
        title: string;
        isCloseAffordance: boolean;
        handle: number;
    }[]): Thenable<number>;
    disable(): void;
    private showMessage(severity, message, commands);
    private showModalMessage(severity, message, commands);
}
