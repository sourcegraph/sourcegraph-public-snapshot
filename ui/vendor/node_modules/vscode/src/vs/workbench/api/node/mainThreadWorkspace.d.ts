import URI from 'vs/base/common/uri';
import { ISearchService } from 'vs/platform/search/common/search';
import { IWorkspaceContextService } from 'vs/platform/workspace/common/workspace';
import { IThreadService } from 'vs/workbench/services/thread/common/threadService';
import { IResourceEdit } from 'vs/editor/common/services/bulkEdit';
import { TPromise } from 'vs/base/common/winjs.base';
import { Uri } from 'vscode';
import { MainThreadWorkspaceShape } from './extHost.protocol';
export declare class MainThreadWorkspace extends MainThreadWorkspaceShape {
    private _activeSearches;
    private _searchService;
    private _contextService;
    private _textFileService;
    private _editorService;
    private _textModelResolverService;
    private _eventService;
    private _threadService;
    private _proxy;
    constructor(searchService: ISearchService, contextService: IWorkspaceContextService, threadService: IThreadService, textFileService: any, editorService: any, textModelResolverService: any, eventService: any);
    $startSearch(include: string, exclude: string, maxResults: number, requestId: number): Thenable<Uri[]>;
    $cancelSearch(requestId: number): Thenable<boolean>;
    $saveAll(includeUntitled?: boolean): Thenable<boolean>;
    $applyWorkspaceEdit(edits: IResourceEdit[]): TPromise<boolean>;
    $setWorkspace(resource: URI, state?: {
        commitID?: string;
        branch?: string;
        zapRef?: string;
    }): TPromise<void>;
    $setWorkspaceState(state?: {
        commitID?: string;
        branch?: string;
        zapRef?: string;
    }): TPromise<void>;
}
