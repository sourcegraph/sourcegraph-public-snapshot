import { IProgressService2 } from 'vs/platform/progress/common/progress';
import { MainThreadProgressShape } from './extHost.protocol';
export declare class MainThreadProgress extends MainThreadProgressShape {
    private _progressService;
    private progress;
    constructor(progressService: IProgressService2);
    $startWindow(handle: number, title: string): void;
    $startScm(handle: number): void;
    private _createTask(handle);
    $progressReport(handle: number, message: any): void;
    $progressEnd(handle: number): void;
}
