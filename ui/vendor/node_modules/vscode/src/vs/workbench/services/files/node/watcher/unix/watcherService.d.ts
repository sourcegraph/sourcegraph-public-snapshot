import { FileChangesEvent } from 'vs/platform/files/common/files';
export declare class FileWatcher {
    private basePath;
    private ignored;
    private onFileChanges;
    private errorLogger;
    private verboseLogging;
    private static MAX_RESTARTS;
    private isDisposed;
    private restartCounter;
    constructor(basePath: string, ignored: string[], onFileChanges: (changes: FileChangesEvent) => void, errorLogger: (msg: string) => void, verboseLogging: boolean);
    startWatching(): () => void;
    private onRawFileEvents(events);
}
