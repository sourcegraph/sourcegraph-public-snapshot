import { Theme, IThemeRule } from 'vs/editor/common/modes/supports/tokenization';
export declare var IStandaloneColorService: {
    (...args: any[]): void;
    type: IStandaloneColorService;
};
export declare type BuiltinTheme = 'vs' | 'vs-dark' | 'hc-black';
export interface ITheme {
    base: BuiltinTheme;
    inherit: boolean;
    rules: IThemeRule[];
}
export interface IStandaloneColorService {
    _serviceBrand: any;
    setTheme(themeName: string): string;
    defineTheme(themeName: string, themeData: ITheme): void;
    getTheme(): Theme;
}
