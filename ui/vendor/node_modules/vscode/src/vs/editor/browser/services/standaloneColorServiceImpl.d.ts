import { Theme } from 'vs/editor/common/modes/supports/tokenization';
import { IStandaloneColorService, ITheme } from 'vs/editor/common/services/standaloneColorService';
export declare class StandaloneColorServiceImpl implements IStandaloneColorService {
    _serviceBrand: any;
    private _knownThemes;
    private _styleElement;
    private _theme;
    constructor();
    defineTheme(themeName: string, themeData: ITheme): void;
    getTheme(): Theme;
    setTheme(themeName: string): string;
}
