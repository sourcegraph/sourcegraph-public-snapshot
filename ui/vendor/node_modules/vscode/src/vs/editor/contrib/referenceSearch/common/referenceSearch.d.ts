import URI from 'vs/base/common/uri';
import { TPromise } from 'vs/base/common/winjs.base';
import { IReadOnlyModel } from 'vs/editor/common/editorCommon';
import { Location, IReferenceInformation, ISymbolDescriptor, ReferenceContext } from 'vs/editor/common/modes';
import { Position } from 'vs/editor/common/core/position';
export declare function provideReferences(model: IReadOnlyModel, position: Position, progress: (locations: Location[]) => void, context?: ReferenceContext): TPromise<Location[]>;
export declare function provideWorkspaceReferences(modeId: string, workspace: URI, query: ISymbolDescriptor, hints: {
    [hint: string]: any;
}, progress: (references: IReferenceInformation[]) => void): TPromise<IReferenceInformation[]>;
