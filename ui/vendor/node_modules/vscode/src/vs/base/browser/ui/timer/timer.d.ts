import './timer.css';
export declare class TimeKeeperRenderer {
    private listenersToRemove;
    private timeKeeper;
    private outerDomNode;
    private domNode;
    private renderCnt;
    private lastEventIndex;
    private textFilter;
    private textFilterDomNode;
    private timeFilter;
    private timeFilterDomNode;
    private intervalTokenId;
    private renderedEvents;
    private onHide;
    constructor(onHide: () => void);
    destroy(): void;
    private _createDomNode();
    private onTextFilterChange();
    private onTimeFilterChange();
    private matchesTextFilter(event);
    private matchesTimeFilter(event);
    private shouldShow(event);
    private refilter();
    private _onClear();
    private leftPaddedString(size, padChar, str);
    private rightPaddedString(size, padChar, str);
    private renderRow(topic, name, timeTook, timeStart, timerEnd);
    private _suffix0(s);
    private _twoPrecision(a);
    private _absoluteTime(t);
    private _renderEvent(domNode, event);
    private _renderStartTimerEvent(event);
    private _render();
    private _repeatStr(str, cnt);
}
