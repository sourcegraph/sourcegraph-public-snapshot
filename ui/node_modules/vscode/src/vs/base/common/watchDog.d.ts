import Event from 'vs/base/common/event';
export declare class WatchDog {
    private _timeout;
    private _threshold;
    private _onAlert;
    private _handle;
    private _missed;
    private _lastSignal;
    constructor(timeout: number, threshold: number);
    dispose(): void;
    readonly onAlert: Event<this>;
    start(): void;
    stop(): void;
    reset(): void;
    private _check();
}
