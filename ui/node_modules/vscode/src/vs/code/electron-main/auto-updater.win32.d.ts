import { EventEmitter } from 'events';
import { TPromise } from 'vs/base/common/winjs.base';
import { ILifecycleService } from 'vs/code/electron-main/lifecycle';
import { IRequestService } from 'vs/platform/request/common/request';
export interface IUpdate {
    url: string;
    name: string;
    releaseNotes?: string;
    version?: string;
    hash?: string;
}
export declare class Win32AutoUpdaterImpl extends EventEmitter {
    private lifecycleService;
    private requestService;
    private url;
    private currentRequest;
    constructor(lifecycleService: ILifecycleService, requestService: IRequestService);
    readonly cachePath: TPromise<string>;
    setFeedURL(url: string): void;
    checkForUpdates(): void;
    private getUpdatePackagePath(version);
    private quitAndUpdate(updatePackagePath);
    private cleanup(exceptVersion?);
}
