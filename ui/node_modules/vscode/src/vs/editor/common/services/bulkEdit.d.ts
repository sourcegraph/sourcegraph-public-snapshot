import URI from 'vs/base/common/uri';
import { TPromise } from 'vs/base/common/winjs.base';
import { ITextModelResolverService } from 'vs/editor/common/services/resolverService';
import { IEventService } from 'vs/platform/event/common/event';
import { IRange, ISelection, ICommonCodeEditor } from 'vs/editor/common/editorCommon';
import { IProgressRunner } from 'vs/platform/progress/common/progress';
export interface IResourceEdit {
    resource: URI;
    range?: IRange;
    newText: string;
}
export interface BulkEdit {
    progress(progress: IProgressRunner): any;
    add(edit: IResourceEdit[]): void;
    finish(): TPromise<ISelection>;
}
export declare function bulkEdit(eventService: IEventService, textModelResolverService: ITextModelResolverService, editor: ICommonCodeEditor, edits: IResourceEdit[], progress?: IProgressRunner): TPromise<any>;
export declare function createBulkEdit(eventService: IEventService, textModelResolverService: ITextModelResolverService, editor: ICommonCodeEditor): BulkEdit;
