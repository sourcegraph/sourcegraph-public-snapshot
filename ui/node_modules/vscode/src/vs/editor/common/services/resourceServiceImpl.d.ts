import URI from 'vs/base/common/uri';
import { IResourceService, ICompatMirrorModel } from 'vs/editor/common/services/resourceService';
export declare class ResourceService implements IResourceService {
    _serviceBrand: any;
    private _map;
    constructor();
    private static _anonymousModelId(input);
    insert(uri: URI, element: ICompatMirrorModel): void;
    get(uri: URI): ICompatMirrorModel;
    remove(uri: URI): void;
}
