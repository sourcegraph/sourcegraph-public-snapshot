import * as modes from 'vs/editor/common/modes';
export declare class FrankensteinMode implements modes.IMode {
    private _modeId;
    constructor(descriptor: modes.IModeDescriptor);
    getId(): string;
}
