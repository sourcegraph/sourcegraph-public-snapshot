import { IState, ILineTokens } from 'vs/editor/common/modes';
import { ITokenizationResult } from 'vs/editor/common/modes/abstractState';
import { LineStream } from 'vs/editor/common/modes/lineStream';
export declare class NullState implements IState {
    private modeId;
    private stateData;
    constructor(modeId: string, stateData: IState);
    clone(): IState;
    equals(other: IState): boolean;
    getModeId(): string;
    tokenize(stream: LineStream): ITokenizationResult;
    getStateData(): IState;
    setStateData(stateData: IState): void;
}
export declare const NULL_MODE_ID: string;
export declare function nullTokenize(modeId: string, buffer: string, state: IState, deltaOffset?: number, stopAtOffset?: number): ILineTokens;
