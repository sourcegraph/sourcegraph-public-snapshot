import { IDisposable } from 'vs/base/common/lifecycle';
import Event from 'vs/base/common/event';
import { IPosition } from 'vs/editor/common/editorCommon';
import { ICodeEditor, IContentWidget, IContentWidgetPosition } from 'vs/editor/browser/editorBrowser';
export declare class LightBulbWidget implements IContentWidget, IDisposable {
    private _editor;
    private _position;
    private _domNode;
    private _visible;
    private _onClick;
    private _toDispose;
    constructor(editor: ICodeEditor);
    dispose(): void;
    readonly onClick: Event<IPosition>;
    getId(): string;
    readonly allowEditorOverflow: boolean;
    getDomNode(): HTMLElement;
    getPosition(): IContentWidgetPosition;
    show(where: IPosition): void;
    hide(): void;
}
