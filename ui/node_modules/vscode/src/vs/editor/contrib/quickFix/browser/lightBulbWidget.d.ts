import './lightBulbWidget.css';
import { IDisposable } from 'vs/base/common/lifecycle';
import Event from 'vs/base/common/event';
import { IPosition } from 'vs/editor/common/editorCommon';
import { ICodeEditor, IOverlayWidget, IOverlayWidgetPosition } from 'vs/editor/browser/editorBrowser';
import { QuickFixComputeEvent } from './quickFixModel';
export declare class LightBulbWidget implements IOverlayWidget, IDisposable {
    private _editor;
    private _position;
    private _domNode;
    private _visible;
    private _onClick;
    private _model;
    private _toDispose;
    constructor(editor: ICodeEditor);
    dispose(): void;
    readonly onClick: Event<{
        x: number;
        y: number;
    }>;
    getId(): string;
    getDomNode(): HTMLElement;
    getPosition(): IOverlayWidgetPosition;
    model: QuickFixComputeEvent;
    show(where: IPosition): void;
    private _hasSpaceInGlyphMargin(line);
    private _layout();
    hide(): void;
}
