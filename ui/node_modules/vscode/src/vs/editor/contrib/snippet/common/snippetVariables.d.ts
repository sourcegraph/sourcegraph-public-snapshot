import * as editorCommon from 'vs/editor/common/editorCommon';
import { ISnippetVariableResolver } from './snippet';
export declare class SnippetVariablesResolver implements ISnippetVariableResolver {
    private _editor;
    constructor(editor: editorCommon.ICommonCodeEditor);
    resolve(name: string): string;
    private _tmCurrentLine();
    private _tmCurrentWord();
    private _tmFilename();
    private _tmDirectory();
    private _tmFilepath();
    private _tmLineIndex();
    private _tmLineNumber();
    private _tmSelectedText();
}
