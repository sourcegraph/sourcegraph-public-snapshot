import { TPromise } from 'vs/base/common/winjs.base';
import { IChannel } from 'vs/base/parts/ipc/common/ipc';
import { IBackupService } from './backup';
export interface IBackupChannel extends IChannel {
    call(command: 'getBackupPath', arg: [number]): TPromise<string>;
    call(command: string, arg?: any): TPromise<any>;
}
export declare class BackupChannel implements IBackupChannel {
    private service;
    constructor(service: IBackupService);
    call(command: string, arg?: any): TPromise<any>;
}
export declare class BackupChannelClient implements IBackupService {
    private channel;
    _serviceBrand: any;
    constructor(channel: IBackupChannel);
    getBackupPath(windowId: number): TPromise<string>;
}
