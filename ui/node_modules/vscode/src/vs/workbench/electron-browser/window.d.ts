import { IPartService } from 'vs/workbench/services/part/common/partService';
import { IWorkbenchEditorService } from 'vs/workbench/services/editor/common/editorService';
import { IEditorGroupService } from 'vs/workbench/services/group/common/groupService';
export declare class ElectronWindow {
    private editorService;
    private editorGroupService;
    private partService;
    private win;
    private windowId;
    constructor(win: Electron.BrowserWindow, shellContainer: HTMLElement, editorService: IWorkbenchEditorService, editorGroupService: IEditorGroupService, partService: IPartService);
    private registerListeners();
    private includesFolder(resources);
    openNew(): void;
    close(): void;
    reload(): void;
    showMessageBox(options: Electron.ShowMessageBoxOptions): number;
    showSaveDialog(options: Electron.SaveDialogOptions, callback?: (fileName: string) => void): string;
    setFullScreen(fullscreen: boolean): void;
    openDevTools(): void;
    setMenuBarVisibility(visible: boolean): void;
    focus(): void;
    flashFrame(): void;
}
