import { TPromise } from 'vs/base/common/winjs.base';
import { ParsedArgs } from 'vs/platform/environment/common/environment';
import { IOpenFileRequest } from 'vs/workbench/electron-browser/common';
export interface IWindowConfiguration extends ParsedArgs, IOpenFileRequest {
    appRoot: string;
    execPath: string;
    userEnv: any;
    workspacePath?: string;
    zoomLevel?: number;
    fullscreen?: boolean;
}
export declare function startup(configuration: IWindowConfiguration): TPromise<void>;
