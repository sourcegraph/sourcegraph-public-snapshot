import { TPromise } from 'vs/base/common/winjs.base';
import { IWorkspaceContextService } from 'vs/platform/workspace/common/workspace';
import { IEnvironmentService } from 'vs/platform/environment/common/environment';
import { ITextFileService } from 'vs/workbench/services/textfile/common/textfiles';
import { IConfigurationService } from 'vs/platform/configuration/common/configuration';
import { IFileService } from 'vs/platform/files/common/files';
import { IConfigurationEditingService, ConfigurationTarget, IConfigurationValue, IConfigurationEditingOptions } from 'vs/workbench/services/configuration/common/configurationEditing';
import { ITextModelResolverService } from 'vs/editor/common/services/resolverService';
export declare class ConfigurationEditingService implements IConfigurationEditingService {
    private configurationService;
    private contextService;
    private environmentService;
    private fileService;
    private textModelResolverService;
    private textFileService;
    _serviceBrand: any;
    private queue;
    constructor(configurationService: IConfigurationService, contextService: IWorkspaceContextService, environmentService: IEnvironmentService, fileService: IFileService, textModelResolverService: ITextModelResolverService, textFileService: ITextFileService);
    writeConfiguration(target: ConfigurationTarget, value: IConfigurationValue, options?: IConfigurationEditingOptions): TPromise<void>;
    private doWriteConfiguration(target, value, options);
    private writeToBuffer(contents, operation, resource, options);
    private applyEditsToBuffer(edit, model);
    private writeToDisk(contents, operation, resource);
    private wrapError(code, target);
    private toErrorMessage(error, target);
    private applyEdits(content, edit);
    private getEdits(content, edit);
    private validate(target, operation, options);
    private resolveContent(resource, options);
    private getConfigurationEditOperation(target, config);
}
