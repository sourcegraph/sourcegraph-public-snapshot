import { IFileService } from 'vs/platform/files/common/files';
import { ITelemetryService } from 'vs/platform/telemetry/common/telemetry';
import { IWorkspaceContextService } from 'vs/platform/workspace/common/workspace';
import { IOptions } from 'vs/workbench/common/options';
export declare class WorkspaceStats {
    private fileService;
    private contextService;
    private telemetryService;
    constructor(fileService: IFileService, contextService: IWorkspaceContextService, telemetryService: ITelemetryService);
    private searchArray(arr, regEx);
    private getWorkspaceTags(workbenchOptions);
    private findFolder({filesToOpen, filesToCreate, filesToDiff});
    private parentURI(uri);
    reportWorkspaceTags(workbenchOptions: IOptions): void;
    private stripLowLevelDomains(domain);
    private extractDomain(url);
    /**
     * Public for testing.
     */
    getDomainsOfRemotes(text: any): string[];
    private reportRemotes(workspaceUri);
    private reportAzureNode(workspaceUri, tags);
    private reportAzureJava(workspaceUri, tags);
    private reportAzure(uri);
    reportCloudStats(): void;
}
