import { IThemeDocument } from 'vs/workbench/services/themes/common/themeService';
export declare class TokenStylesContribution {
    contributeStyles(themeId: string, themeDocument: IThemeDocument, cssRules: string[]): void;
    private _settingsToStatements(settings);
}
export declare class EditorStylesContribution {
    contributeStyles(themeId: string, themeDocument: IThemeDocument, cssRules: string[]): void;
}
export declare class SearchViewStylesContribution {
    contributeStyles(themeId: string, themeDocument: IThemeDocument, cssRules: string[]): void;
}
export declare class TerminalStylesContribution {
    private static ansiColorMap;
    /**
     * Converts a CSS hex color (#rrggbb) to a CSS rgba color (rgba(r, g, b, a)).
     */
    private _convertHexCssColorToRgba(hex, alpha);
    contributeStyles(themeId: string, themeDocument: IThemeDocument, cssRules: string[]): void;
}
