import { TPromise } from 'vs/base/common/winjs.base';
import { IExtensionService } from 'vs/platform/extensions/common/extensions';
import { IThemeService, IThemeData } from 'vs/workbench/services/themes/common/themeService';
import { IWindowService } from 'vs/workbench/services/window/electron-browser/windowService';
import { IStorageService } from 'vs/platform/storage/common/storage';
import { ITelemetryService } from 'vs/platform/telemetry/common/telemetry';
import Event from 'vs/base/common/event';
export declare class ThemeService implements IThemeService {
    private extensionService;
    private windowService;
    private storageService;
    private telemetryService;
    _serviceBrand: any;
    private knownColorThemes;
    private currentColorTheme;
    private container;
    private onColorThemeChange;
    private knownIconThemes;
    private currentIconTheme;
    constructor(extensionService: IExtensionService, windowService: IWindowService, storageService: IStorageService, telemetryService: ITelemetryService);
    readonly onDidColorThemeChange: Event<string>;
    initialize(container: HTMLElement): TPromise<void>;
    setColorTheme(themeId: string, broadcastToAllWindows: boolean): TPromise<boolean>;
    getColorTheme(): string;
    private findThemeData(themeId, defaultId?);
    private applyThemeCSS(themeId, defaultId, onApply);
    getColorThemes(): TPromise<IThemeData[]>;
    private onThemes(extensionFolderPath, extensionId, themes, collector);
    private onIconThemes(extensionFolderPath, extensionId, iconThemes, collector);
    private themeExtensionsActivated;
    private sendTelemetry(themeData);
    getFileIconThemes(): TPromise<IThemeData[]>;
    getFileIconTheme(): string;
    setFileIconTheme(iconTheme: string, broadcastToAllWindows: boolean): TPromise<boolean>;
    private _updateIconTheme(onApply);
}
