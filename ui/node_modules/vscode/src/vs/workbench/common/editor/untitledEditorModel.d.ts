import { TPromise } from 'vs/base/common/winjs.base';
import { EditorModel, IEncodingSupport } from 'vs/workbench/common/editor';
import { StringEditorModel } from 'vs/workbench/common/editor/stringEditorModel';
import URI from 'vs/base/common/uri';
import { IConfigurationService } from 'vs/platform/configuration/common/configuration';
import { IModeService } from 'vs/editor/common/services/modeService';
import { IModelService } from 'vs/editor/common/services/modelService';
import { IMode } from 'vs/editor/common/modes';
import Event from 'vs/base/common/event';
export declare class UntitledEditorModel extends StringEditorModel implements IEncodingSupport {
    private configurationService;
    private textModelChangeListener;
    private configurationChangeListener;
    private dirty;
    private _onDidChangeDirty;
    private _onDidChangeEncoding;
    private configuredEncoding;
    private preferredEncoding;
    private hasAssociatedFilePath;
    constructor(value: string, modeId: string, resource: URI, hasAssociatedFilePath: boolean, modeService: IModeService, modelService: IModelService, configurationService: IConfigurationService);
    readonly onDidChangeDirty: Event<void>;
    readonly onDidChangeEncoding: Event<void>;
    protected getOrCreateMode(modeService: IModeService, modeId: string, firstLineText?: string): TPromise<IMode>;
    private registerListeners();
    private onConfigurationChange(configuration);
    getValue(): string;
    getModeId(): string;
    getEncoding(): string;
    setEncoding(encoding: string): void;
    isDirty(): boolean;
    revert(): void;
    load(): TPromise<EditorModel>;
    private onModelContentChanged();
    dispose(): void;
}
