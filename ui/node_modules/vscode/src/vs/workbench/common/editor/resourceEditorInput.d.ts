import { TPromise } from 'vs/base/common/winjs.base';
import { EditorInput, ITextEditorModel } from 'vs/workbench/common/editor';
import URI from 'vs/base/common/uri';
import { IReference } from 'vs/base/common/lifecycle';
import { ITextModelResolverService } from 'vs/editor/common/services/resolverService';
import { ResourceEditorModel } from 'vs/workbench/common/editor/resourceEditorModel';
/**
 * A read-only text editor input whos contents are made of the provided resource that points to an existing
 * code editor model.
 */
export declare class ResourceEditorInput extends EditorInput {
    private textModelResolverService;
    static ID: string;
    protected promise: TPromise<IReference<ResourceEditorModel>>;
    protected resource: URI;
    private name;
    private description;
    constructor(name: string, description: string, resource: URI, textModelResolverService: ITextModelResolverService);
    getTypeId(): string;
    getName(): string;
    setName(name: string): void;
    getDescription(): string;
    setDescription(description: string): void;
    getTelemetryDescriptor(): {
        [key: string]: any;
    };
    resolve(refresh?: boolean): TPromise<ITextEditorModel>;
    matches(otherInput: any): boolean;
    dispose(): void;
}
