import { TPromise } from 'vs/base/common/winjs.base';
import URI from 'vs/base/common/uri';
import { UntitledEditorInput as AbstractUntitledEditorInput, EncodingMode, ConfirmResult } from 'vs/workbench/common/editor';
import { UntitledEditorModel } from 'vs/workbench/common/editor/untitledEditorModel';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
import { IWorkspaceContextService } from 'vs/platform/workspace/common/workspace';
import { IModeService } from 'vs/editor/common/services/modeService';
import Event from 'vs/base/common/event';
import { ITextFileService } from 'vs/workbench/parts/files/common/files';
/**
 * An editor input to be used for untitled text buffers.
 */
export declare class UntitledEditorInput extends AbstractUntitledEditorInput {
    private instantiationService;
    private contextService;
    private modeService;
    private textFileService;
    static ID: string;
    static SCHEMA: string;
    private resource;
    private hasAssociatedFilePath;
    private modeId;
    private cachedModel;
    private _onDidModelChangeEncoding;
    private toUnbind;
    constructor(resource: URI, hasAssociatedFilePath: boolean, modeId: string, instantiationService: IInstantiationService, contextService: IWorkspaceContextService, modeService: IModeService, textFileService: ITextFileService);
    readonly onDidModelChangeEncoding: Event<void>;
    getTypeId(): string;
    getResource(): URI;
    getName(): string;
    getDescription(): string;
    isDirty(): boolean;
    confirmSave(): ConfirmResult;
    save(): TPromise<boolean>;
    revert(): TPromise<boolean>;
    suggestFileName(): string;
    getMime(): string;
    getEncoding(): string;
    setEncoding(encoding: string, mode: EncodingMode): void;
    resolve(refresh?: boolean): TPromise<UntitledEditorModel>;
    private createModel();
    matches(otherInput: any): boolean;
    dispose(): void;
}
