import './media/activitybarpart.css';
import { Builder } from 'vs/base/browser/builder';
import { Part } from 'vs/workbench/browser/part';
import { IViewletService } from 'vs/workbench/services/viewlet/common/viewletService';
import { IActivityService, IBadge } from 'vs/workbench/services/activity/common/activityService';
import { IPartService } from 'vs/workbench/services/part/common/partService';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
import { IKeybindingService } from 'vs/platform/keybinding/common/keybinding';
export declare class ActivitybarPart extends Part implements IActivityService {
    private viewletService;
    private keybindingService;
    private instantiationService;
    private partService;
    _serviceBrand: any;
    private viewletSwitcherBar;
    private activityActionItems;
    private compositeIdToActions;
    constructor(id: string, viewletService: IViewletService, keybindingService: IKeybindingService, instantiationService: IInstantiationService, partService: IPartService);
    private registerListeners();
    private onActiveCompositeChanged(composite);
    private onCompositeClosed(composite);
    showActivity(compositeId: string, badge: IBadge, clazz?: string): void;
    clearActivity(compositeId: string): void;
    createContentArea(parent: Builder): Builder;
    private createViewletSwitcher(div);
    private toAction(composite);
    private getKeybindingLabel(id);
    dispose(): void;
}
