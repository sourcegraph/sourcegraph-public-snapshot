import URI from 'vs/base/common/uri';
import { TPromise } from 'vs/base/common/winjs.base';
import { IThreadService } from 'vs/workbench/services/thread/common/threadService';
import { ISingleEditOperation, ISelection, IRange, IDecorationRenderOptions, IDecorationOptions } from 'vs/editor/common/editorCommon';
import { ICodeEditorService } from 'vs/editor/common/services/codeEditorService';
import { IWorkbenchEditorService } from 'vs/workbench/services/editor/common/editorService';
import { IEditorGroupService } from 'vs/workbench/services/group/common/groupService';
import { Position as EditorPosition } from 'vs/platform/editor/common/editor';
import { IModelService } from 'vs/editor/common/services/modelService';
import { TextEditorRevealType, IApplyEditsOptions, ITextEditorConfigurationUpdate } from 'vs/workbench/api/node/mainThreadEditorsTracker';
import { ITelemetryService } from 'vs/platform/telemetry/common/telemetry';
import { IEventService } from 'vs/platform/event/common/event';
import { MainThreadEditorsShape } from './extHost.protocol';
export declare class MainThreadEditors extends MainThreadEditorsShape {
    private _proxy;
    private _workbenchEditorService;
    private _telemetryService;
    private _editorTracker;
    private _toDispose;
    private _textEditorsListenersMap;
    private _textEditorsMap;
    private _activeTextEditor;
    private _visibleEditors;
    private _editorPositionData;
    constructor(threadService: IThreadService, workbenchEditorService: IWorkbenchEditorService, editorGroupService: IEditorGroupService, telemetryService: ITelemetryService, editorService: ICodeEditorService, eventService: IEventService, modelService: IModelService);
    dispose(): void;
    private _onTextEditorAdd(textEditor);
    private _onTextEditorRemove(textEditor);
    private _updateActiveAndVisibleTextEditors();
    private _findActiveTextEditorId();
    private _findEditorPosition(editor);
    private _getTextEditorPositionData();
    $tryShowTextDocument(resource: URI, position: EditorPosition, preserveFocus: boolean): TPromise<string>;
    $tryShowEditor(id: string, position: EditorPosition): TPromise<void>;
    $tryHideEditor(id: string): TPromise<void>;
    $trySetSelections(id: string, selections: ISelection[]): TPromise<any>;
    $trySetDecorations(id: string, key: string, ranges: IDecorationOptions[]): TPromise<any>;
    $tryRevealRange(id: string, range: IRange, revealType: TextEditorRevealType): TPromise<any>;
    $trySetOptions(id: string, options: ITextEditorConfigurationUpdate): TPromise<any>;
    $tryApplyEdits(id: string, modelVersionId: number, edits: ISingleEditOperation[], opts: IApplyEditsOptions): TPromise<boolean>;
    $registerTextEditorDecorationType(key: string, options: IDecorationRenderOptions): void;
    $removeTextEditorDecorationType(key: string): void;
}
