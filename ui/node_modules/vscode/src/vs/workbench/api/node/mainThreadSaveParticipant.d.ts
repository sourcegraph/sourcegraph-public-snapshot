import { TPromise } from 'vs/base/common/winjs.base';
import { IThreadService } from 'vs/workbench/services/thread/common/threadService';
import { ISaveParticipant, ITextFileEditorModel } from 'vs/workbench/parts/files/common/files';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
import { SaveReason } from 'vs/workbench/parts/files/common/files';
export declare class SaveParticipant implements ISaveParticipant {
    private _saveParticipants;
    constructor(instantiationService: IInstantiationService, threadService: IThreadService);
    participate(model: ITextFileEditorModel, env: {
        reason: SaveReason;
    }): TPromise<any>;
}
