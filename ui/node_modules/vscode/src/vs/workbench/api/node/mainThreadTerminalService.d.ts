import { ITerminalService } from 'vs/workbench/parts/terminal/electron-browser/terminal';
import { IThreadService } from 'vs/workbench/services/thread/common/threadService';
import { TPromise } from 'vs/base/common/winjs.base';
import { MainThreadTerminalServiceShape } from './extHost.protocol';
export declare class MainThreadTerminalService extends MainThreadTerminalServiceShape {
    private terminalService;
    private _proxy;
    private _toDispose;
    constructor(threadService: IThreadService, terminalService: ITerminalService);
    dispose(): void;
    $createTerminal(name?: string, shellPath?: string, shellArgs?: string[]): TPromise<number>;
    $show(terminalId: number, preserveFocus: boolean): void;
    $hide(terminalId: number): void;
    $dispose(terminalId: number): void;
    $sendText(terminalId: number, text: string, addNewLine: boolean): void;
    private _onTerminalDisposed(terminalInstance);
    private _onTerminalProcessIdReady(terminalInstance);
}
