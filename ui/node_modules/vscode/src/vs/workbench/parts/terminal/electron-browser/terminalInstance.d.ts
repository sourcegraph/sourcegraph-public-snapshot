import Event from 'vs/base/common/event';
import lifecycle = require('vs/base/common/lifecycle');
import { IContextKeyService, IContextKey } from 'vs/platform/contextkey/common/contextkey';
import { IKeybindingService } from 'vs/platform/keybinding/common/keybinding';
import { IMessageService } from 'vs/platform/message/common/message';
import { IStringDictionary } from 'vs/base/common/collections';
import { ITerminalInstance, IShell } from 'vs/workbench/parts/terminal/common/terminal';
import { IWorkspace } from 'vs/platform/workspace/common/workspace';
import { TerminalConfigHelper } from 'vs/workbench/parts/terminal/electron-browser/terminalConfigHelper';
export declare class TerminalInstance implements ITerminalInstance {
    private _terminalFocusContextKey;
    private _configHelper;
    private _container;
    private _contextKeyService;
    private _keybindingService;
    private _messageService;
    /** The amount of time to consider terminal errors to be related to the launch */
    private static readonly LAUNCHING_DURATION;
    private static readonly EOL_REGEX;
    private static _idCounter;
    private _id;
    private _isExiting;
    private _hadFocusOnExit;
    private _isLaunching;
    private _isVisible;
    private _onDisposed;
    private _onProcessIdReady;
    private _onTitleChanged;
    private _process;
    private _processId;
    private _skipTerminalCommands;
    private _title;
    private _toDispose;
    private _wrapperElement;
    private _xterm;
    private _xtermElement;
    private _terminalHasTextContextKey;
    readonly id: number;
    readonly processId: number;
    readonly onClosed: Event<TerminalInstance>;
    readonly onProcessIdReady: Event<TerminalInstance>;
    readonly onTitleChanged: Event<string>;
    readonly title: string;
    readonly hadFocusOnExit: boolean;
    constructor(_terminalFocusContextKey: IContextKey<boolean>, _configHelper: TerminalConfigHelper, _container: HTMLElement, workspace: IWorkspace, name: string, shell: IShell, _contextKeyService: IContextKeyService, _keybindingService: IKeybindingService, _messageService: IMessageService);
    addDisposable(disposable: lifecycle.IDisposable): void;
    attachToElement(container: HTMLElement): void;
    copySelection(): void;
    dispose(): void;
    focus(force?: boolean): void;
    paste(): void;
    sendText(text: string, addNewLine: boolean): void;
    setVisible(visible: boolean): void;
    scrollDownLine(): void;
    scrollDownPage(): void;
    scrollToBottom(): void;
    scrollUpLine(): void;
    scrollUpPage(): void;
    scrollToTop(): void;
    clear(): void;
    private _refreshSelectionContextKey();
    private _sanitizeInput(data);
    private _createProcess(workspace, name, shell);
    static createTerminalEnv(parentEnv: IStringDictionary<string>, shell: IShell, workspace: IWorkspace, locale?: string): IStringDictionary<string>;
    private static _sanitizeCwd(cwd);
    private static _cloneEnv(env);
    private static _getLangEnvVariable(locale);
    setCursorBlink(blink: boolean): void;
    setCommandsToSkipShell(commands: string[]): void;
    setScrollback(lineCount: number): void;
    layout(dimension: {
        width: number;
        height: number;
    }): void;
}
