import { Action, IAction } from 'vs/base/common/actions';
import { Builder, Dimension } from 'vs/base/browser/builder';
import { IActionItem } from 'vs/base/browser/ui/actionbar/actionbar';
import { IConfigurationService } from 'vs/platform/configuration/common/configuration';
import { IContextMenuService } from 'vs/platform/contextview/browser/contextView';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
import { IKeybindingService } from 'vs/platform/keybinding/common/keybinding';
import { ITelemetryService } from 'vs/platform/telemetry/common/telemetry';
import { ITerminalService } from 'vs/workbench/parts/terminal/electron-browser/terminal';
import { IThemeService } from 'vs/workbench/services/themes/common/themeService';
import { Panel } from 'vs/workbench/browser/panel';
import { TPromise } from 'vs/base/common/winjs.base';
export declare class TerminalPanel extends Panel {
    private _configurationService;
    private _contextMenuService;
    private _instantiationService;
    private _keybindingService;
    private _terminalService;
    private _themeService;
    private _actions;
    private _contextMenuActions;
    private _currentBaseThemeId;
    private _font;
    private _fontStyleElement;
    private _parentDomElement;
    private _terminalContainer;
    private _themeStyleElement;
    constructor(_configurationService: IConfigurationService, _contextMenuService: IContextMenuService, _instantiationService: IInstantiationService, _keybindingService: IKeybindingService, _terminalService: ITerminalService, _themeService: IThemeService, telemetryService: ITelemetryService);
    create(parent: Builder): TPromise<any>;
    layout(dimension?: Dimension): void;
    setVisible(visible: boolean): TPromise<void>;
    getActions(): IAction[];
    private _getContextMenuActions();
    getActionItem(action: Action): IActionItem;
    focus(): void;
    private _attachEventListeners();
    private _updateTheme(themeId?);
    /**
     * Converts a CSS hex color (#rrggbb) to a CSS rgba color (rgba(r, g, b, a)).
     */
    private _convertHexCssColorToRgba(hex, alpha);
    private _updateConfig();
    private _updateFont();
    private _fontsDiffer(a, b);
    private _updateCursorBlink();
    private _updateCommandsToSkipShell();
}
