import { TPromise } from 'vs/base/common/winjs.base';
import debug = require('vs/workbench/parts/debug/common/debug');
import { IExtensionDescription } from 'vs/platform/extensions/common/extensions';
import { IConfigurationResolverService } from 'vs/workbench/services/configurationResolver/common/configurationResolver';
import { ICommandService } from 'vs/platform/commands/common/commands';
export declare class Adapter {
    rawAdapter: debug.IRawAdapter;
    extensionDescription: IExtensionDescription;
    private commandService;
    runtime: string;
    program: string;
    runtimeArgs: string[];
    args: string[];
    type: string;
    private _label;
    private configurationAttributes;
    initialConfigurations: any[] | string;
    variables: {
        [key: string]: string;
    };
    enableBreakpointsFor: {
        languageIds: string[];
    };
    aiKey: string;
    constructor(rawAdapter: debug.IRawAdapter, extensionDescription: IExtensionDescription, configurationResolverService: IConfigurationResolverService, commandService: ICommandService);
    getInitialConfigurations(): TPromise<string | any[]>;
    label: string;
    getSchemaAttributes(): any[];
    private warnRelativePaths(attribute);
}
