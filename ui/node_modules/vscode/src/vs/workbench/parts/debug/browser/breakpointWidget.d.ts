import '../browser/media/breakpointWidget.css';
import editorbrowser = require('vs/editor/browser/editorBrowser');
import { ZoneWidget } from 'vs/editor/contrib/zoneWidget/browser/zoneWidget';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
import { IContextViewService } from 'vs/platform/contextview/browser/contextView';
import { IContextKeyService } from 'vs/platform/contextkey/common/contextkey';
import debug = require('vs/workbench/parts/debug/common/debug');
export declare class BreakpointWidget extends ZoneWidget {
    private lineNumber;
    private contextViewService;
    private debugService;
    static INSTANCE: BreakpointWidget;
    private inputBox;
    private toDispose;
    private breakpointWidgetVisible;
    private hitCountContext;
    constructor(editor: editorbrowser.ICodeEditor, lineNumber: number, contextViewService: IContextViewService, debugService: debug.IDebugService, contextKeyService: IContextKeyService);
    static createInstance(editor: editorbrowser.ICodeEditor, lineNumber: number, instantiationService: IInstantiationService): void;
    protected _fillContainer(container: HTMLElement): void;
    dispose(): void;
}
