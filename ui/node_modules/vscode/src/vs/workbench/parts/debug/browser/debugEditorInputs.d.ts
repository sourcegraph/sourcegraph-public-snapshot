import { TPromise } from 'vs/base/common/winjs.base';
import strinput = require('vs/workbench/common/editor/stringEditorInput');
import { EditorInput, EditorModel } from 'vs/workbench/common/editor';
import uri from 'vs/base/common/uri';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
export declare class DebugStringEditorInput extends strinput.StringEditorInput {
    private resourceUrl;
    constructor(name: string, resourceUrl: uri, description: string, value: string, modeId: string, singleton: boolean, instantiationService: IInstantiationService);
    getResource(): uri;
}
export declare class DebugErrorEditorInput extends EditorInput {
    private name;
    value: string;
    static ID: string;
    constructor(name: string, value: string);
    getTypeId(): string;
    resolve(refresh?: boolean): TPromise<EditorModel>;
    getName(): string;
}
