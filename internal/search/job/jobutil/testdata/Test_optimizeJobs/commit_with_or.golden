INPUT:

type:commit a or b

BASE:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (OR
        (PARALLEL
          CommitSearchJob
          ComputeExcludedRepos)
        (PARALLEL
          CommitSearchJob
          ComputeExcludedRepos)))))

OPTIMIZED:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (PARALLEL
        CommitSearchJob
        (OR
          (PARALLEL
            NoopJob
            ComputeExcludedRepos)
          (PARALLEL
            NoopJob
            ComputeExcludedRepos))))))
