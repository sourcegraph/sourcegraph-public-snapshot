INPUT:

type:commit a and b

BASE:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (AND
        (LIMIT
          40000
          (PARALLEL
            Commit
            ComputeExcludedRepos))
        (LIMIT
          40000
          (PARALLEL
            Commit
            ComputeExcludedRepos))))))

OPTIMIZED:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (PARALLEL
        Commit
        (AND
          (LIMIT
            40000
            (PARALLEL
              NoopJob
              ComputeExcludedRepos))
          (LIMIT
            40000
            (PARALLEL
              NoopJob
              ComputeExcludedRepos)))))))
