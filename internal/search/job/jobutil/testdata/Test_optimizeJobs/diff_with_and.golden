INPUT:

type:diff a and b

BASE:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (AND
        (LIMIT
          40000
          (PARALLEL
            DiffSearchJob
            ComputeExcludedRepos))
        (LIMIT
          40000
          (PARALLEL
            DiffSearchJob
            ComputeExcludedRepos))))))

OPTIMIZED:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (PARALLEL
        DiffSearchJob
        (AND
          (LIMIT
            40000
            (PARALLEL
              NoopJob
              ComputeExcludedRepos))
          (LIMIT
            40000
            (PARALLEL
              NoopJob
              ComputeExcludedRepos)))))))
