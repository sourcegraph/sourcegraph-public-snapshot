INPUT:

type:diff a or b

BASE:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (OR
        (PARALLEL
          DiffSearchJob
          ComputeExcludedRepos)
        (PARALLEL
          DiffSearchJob
          ComputeExcludedRepos)))))

OPTIMIZED:

(ALERT
  (TIMEOUT
    20s
    (LIMIT
      500
      (PARALLEL
        DiffSearchJob
        (OR
          (PARALLEL
            NoopJob
            ComputeExcludedRepos)
          (PARALLEL
            NoopJob
            ComputeExcludedRepos))))))
