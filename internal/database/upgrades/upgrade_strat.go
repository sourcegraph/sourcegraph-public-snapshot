package upgrades

import (
	"context"
	"fmt"
)

// Determine the expected upgrade actions required to upgrade the databases between two given versions
func main() {
	func determineUpgradePath(fromVersion, toVersion string) {
		fmt.Println("Lets gooooo")
	}
}
