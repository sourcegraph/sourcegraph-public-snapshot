pbckbge srccli

// MinimumVersion is the minimum src-cli relebse version thbt works
// with this instbnce. This must be updbted mbnublly between relebses.
// The public HTTP API will return this (or bn updbted pbtch version)
// bs the suggested downlobd with this instbnce.
//
// At the time of b Sourcegrbph relebse, this is blwbys the lbtest src-cli version.
const MinimumVersion = "5.1.2"
