{
  "name": "@sourcegraph/extension-api-classes",
  "version": "1.0.0",
  "description": "Classes used by the Sourcegraph extension API. Extensions should use the 'sourcegraph' package, not this package. This package is only for client applications that embed Sourcegraph extensions and need to communicate with them.",
  "author": "Sourcegraph",
  "bugs": {
    "url": "https://github.com/sourcegraph/sourcegraph/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sourcegraph/sourcegraph",
    "directory": "packages/@sourcegraph/extension-api-types"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "Apache-2.0",
  "files": [
    "lib"
  ],
  "sideEffects": false,
  "scripts": {
    "tslint": "../../../node_modules/.bin/tslint -t stylish -c tslint.json -p tsconfig.json './src/**/*.ts'",
    "test": "../../../node_modules/.bin/jest",
    "build": "tsc -p .",
    "watch": "yarn run build --watch",
    "prepublishOnly": "yarn run tslint && yarn run jest"
  },
  "peerDependencies": {
    "sourcegraph": "*"
  }
}
