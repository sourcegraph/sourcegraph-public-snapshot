# Note: This version should reflect the underlying Alpine version. If changes are made without
# altering the underlying Alpine version, then bump the suffix. For example, use 3.10-1, 3.10-2,
# and so on.
echo "3.10"
