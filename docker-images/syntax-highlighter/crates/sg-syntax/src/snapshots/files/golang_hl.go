package main

import (
	"fmt"
	"strings"
)

func example(x int) {
	fmt.Println(x)
}

type MyThing struct{}

func main() {
	example(5)

	x := []bool{true}
	x = append(x, false)
	for row, line := range strings.Split(code, "\n") {
		x := strings.Split("hello")
		fmt.Println(row, line)
	}

	y := MyThing{}
	y.DoSomething()
}
