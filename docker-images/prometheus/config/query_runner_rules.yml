groups:
  - name: query-runner
    rules:
      - record: alert_count
        labels:
          service_name: query-runner
          level: critical
        expr: 0
      - record: alert_count
        labels:
          service_name: query-runner
          level: warning
        expr: 0
