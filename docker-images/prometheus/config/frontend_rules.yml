groups:
  - name: frontend
    rules:
      - record: alert_count
        labels:
          service_name: frontend
          level: critical
        expr: 0
      - record: alert_count
        labels:
          service_name: frontend
          level: warning
        expr: 0
