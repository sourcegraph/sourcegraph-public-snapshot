groups:
  - name: repo-updater
    rules:
      - record: alert_count
        labels:
          container: repo-updater
          level: critical
        expr: 0
      - record: alert_count
        labels:
          container: repo-updater
          level: warning
        expr: 0
