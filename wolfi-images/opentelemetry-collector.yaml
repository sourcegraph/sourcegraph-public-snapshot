include: ./sourcegraph-template.yaml

contents:
  packages:
    # Included by existing SG base image
    - tini
    - mailcap

    ## opentelemetry-collector packages
    # Keep in sync with go.opentelemetry.io/collector version in go.mod, and package in wolfi-packages/opentelemetry-collector.yaml
    - opentelemetry-collector=0.92.0-r7@sourcegraph

paths:
  - path: /otel-collector
    type: directory
    permissions: 0o755

work-dir: /otel-collector
