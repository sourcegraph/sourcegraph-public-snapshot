import assert from 'assert' // at least 1 import is necessary to avoid error with tsconfig isolatedModules

describe('search/helpers', () => {
    describe('queryIndexOfScope()', () => {
        it.skip('should return the index of a scope if contained in the query', () => {
            assert.fail('TODO')
            /* TODO */
        })
        it.skip('should return the index of a scope if at the beginning of the query', () => {
            /* TODO */
        })
        it.skip('should return the -1 if the scope is not contained in the query', () => {
            /* TODO */
        })
        it.skip('should return the -1 if the scope is contained as a substring of another scope', () => {
            /* TODO */
        })
    })
})
