{
  "name": "sourcegraph",
  "private": true,
  "scripts": {
    "prettier": "prettier '**/{*.{js?(on),ts?(x),graphql,md},.*.js?(on)}' --write --list-different",
    "precommit": "bash dev/precommit.sh"
  },
  "devDependencies": {
    "@sourcegraph/prettierrc": "^2.1.0",
    "chokidar-cli": "^1.2.0",
    "husky": "^0.14.3",
    "prettier": "^1.9.2"
  }
}
