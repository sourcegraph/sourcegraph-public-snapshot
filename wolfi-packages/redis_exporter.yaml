package:
  name: redis_exporter
  version: 1.59.0
  epoch: 0
  description: "Prometheus Exporter for Redis Metrics"
  target-architecture:
    - x86_64
  copyright:
    - paths:
        - "*"
      attestation: 'Copyright (c) 2016 Oliver'
      license: 'MIT License'
  dependencies:
    runtime:

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - wolfi-base

pipeline:
  - uses: fetch
    with:
      uri: https://github.com/oliver006/redis_exporter/archive/refs/tags/v${{package.version}}.tar.gz
      expected-sha256: 1a48566c22dbe34d49f772fbdea541611a80cc2b3be9e207c7f50b3972f38eb4

  - uses: go/build
    with:
      packages: main.go
      ldflags: "-s -w -extldflags \"-static\""
      prefix: /usr/local
      output: redis_exporter

update:
  enabled: true
  github:
    identifier: oliver006/redis_exporter
    strip-prefix: "v"
