package:
  name: redis_exporter
  version: 1.60.0
  epoch: 0
  description: "Prometheus Exporter for Redis Metrics"
  target-architecture:
    - x86_64
  copyright:
    - paths:
        - "*"
      attestation: 'Copyright (c) 2016 Oliver'
      license: 'MIT License'
  dependencies:
    runtime:

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - wolfi-base

pipeline:
  - uses: fetch
    with:
      uri: https://github.com/oliver006/redis_exporter/archive/refs/tags/v${{package.version}}.tar.gz
      expected-sha256: f85167e86c0daa9a33cc678c98f6b5d60853217ec2bf4edbbdf7b7400b0ec0bd

  - uses: go/build
    with:
      packages: main.go
      ldflags: "-s -w -extldflags \"-static\""
      prefix: /usr/local
      output: redis_exporter

update:
  enabled: true
  github:
    identifier: oliver006/redis_exporter
    strip-prefix: "v"
