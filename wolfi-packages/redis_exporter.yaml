package:
  name: redis_exporter
  version: 1.62.0
  epoch: 0
  description: "Prometheus Exporter for Redis Metrics"
  target-architecture:
    - x86_64
  copyright:
    - paths:
        - "*"
      attestation: 'Copyright (c) 2016 Oliver'
      license: 'MIT License'
  dependencies:
    runtime:

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - wolfi-base

pipeline:
  - uses: fetch
    with:
      uri: https://github.com/oliver006/redis_exporter/archive/refs/tags/v${{package.version}}.tar.gz
      expected-sha256: 6f7ec4bd077ede1831e6eff541f1d0c1d9ce928177bb74507bc55f8b05e4949d

  - uses: go/build
    with:
      packages: main.go
      ldflags: "-s -w -extldflags \"-static\""
      prefix: /usr/local
      output: redis_exporter

update:
  enabled: true
  github:
    identifier: oliver006/redis_exporter
    strip-prefix: "v"
