name: Review
on:
  push:
    branches:
      - tr/cody-review

jobs:
  backport:
    name: Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with: { repository: 'sourcegraph/sourcegraph' }
